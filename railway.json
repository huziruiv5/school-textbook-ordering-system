{
  "build": {
    "builder": "HEROKU",
    "buildCommand": "mvn clean package -DskipTests"
  },
  "deploy": {
    "startCommand": "java -jar target/school-textbook-ordering-system-1.0.0.jar",
    "healthcheckPath": "/login",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE"
  }
}
